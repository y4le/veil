{
  "name": "veil",
  "version": "0.1.0",
  "private": true,
  "description": "Encrypt static HTML sites at build time; decrypt client-side with Web Crypto. Zero runtime dependencies.",
  "main": "veil.js",
  "bin": {
    "veil": "./veil.js"
  },
  "type": "commonjs",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/y4le/veil.git"
  },
  "scripts": {
    "test": "node --test test.js",
    "test:browser": "node --test browser.e2e.js",
    "test:all": "node --test test.js && node --test browser.e2e.js"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
