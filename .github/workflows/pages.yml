name: Deploy demo site

on:
  push:
    branches: [main]
    paths:
      - 'site/**'
      - 'veil.js'
      - 'build-site.sh'
      - '.github/workflows/pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# One deploy at a time; never cancel a running one mid-publish.
concurrency:
  group: pages
  cancel-in-progress: false

# Every action is pinned to a full commit SHA. A movable tag is the one
# dependency that could rewrite the build inputs or the PATH before the step
# that receives the vault passphrase; only a SHA is immutable.
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '24'

      # Encrypts both zones and verifies each one before anything is uploaded;
      # a failed audit fails the job. veil.js is this repository's own file, so
      # there is nothing to vendor or pin here; a site in another repository
      # should pin a commit and check a recorded digest instead.
      - name: Build and verify
        run: ./build-site.sh
        env:
          VEIL_VAULT_PASSPHRASE: ${{ secrets.VEIL_VAULT_PASSPHRASE }}

      - uses: actions/configure-pages@45bfe0192ca1faeb007ade9deae92b16b8254a0d # v6.0.0
      - uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5.0.0
        with:
          path: ./_site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128 # v5.0.0
